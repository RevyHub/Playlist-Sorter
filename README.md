# Playlist-Sorter
CLI tool that fetches a YouTube playlist via the Data API v3 and re-sorts it by date added (newest first), with optional CSV export. Handles pagination automatically and gracefully skips removed/private videos instead of crashing. Minimal setup — just an API key and the requests library.
